{
  "name": "consilium-api",
  "version": "1.0.0",
  "description": "Consilium API",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && export NODE_ENV=production && node dist/server.js",
    "dev": "npm run build && export NODE_ENV=dev && node dist/server.js",
    "test": "tsc && export NODE_ENV=test && mocha --recursive dist/tests --exit"
  },
  "repository": {
    "type": "git"
  },
  "author": "Anson Ervin",
  "license": "MIT",
  "bugs": {},
  "dependencies": {
    "@firebase/firestore": "^2.3.4",
    "@ssense/sscheduler": "1.3.2",
    "ae-backend-database": "^1.1.1",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "firebase": "^8.6.5",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.11.6",
    "@types/supertest": "^2.0.11",
    "@types/twilio": "0.0.10",
    "cors": "^2.8.5",
    "mocha": "^6.0.2",
    "request": "^2.88.0",
    "supertest": "^4.0.2",
    "typescript": "^3.9.7"
  }
}
